import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.qhalikay.app',
  appName: 'qhalikay-app',
  webDir: '../website/dist'
};

export default config;
